# Local-development values only. Use unique secrets outside local development.
POSTGRES_USER=jie
POSTGRES_PASSWORD=jie_local_password
POSTGRES_DB=jie
POSTGRES_PORT=5432

# Keep these credentials and the port synchronized with the values above.
DATABASE_URL=postgresql://jie:jie_local_password@localhost:5432/jie?schema=public
