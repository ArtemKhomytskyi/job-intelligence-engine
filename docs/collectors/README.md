# Collectors

Greenhouse and Lever use public JSON APIs. Generic web sources use bounded HTML extraction and optional browser fallback. All collectors return application-owned candidates to the same normalization, orchestration, and persistence path.

See [generic web extraction](generic-web.md) and the [collection architecture](../architecture/collection.md).
